# TikTok Crawler 环境变量配置示例
# 复制此文件为 .env 并填写实际值

# TikTok Cookie (必需 - 从浏览器获取)
# 获取方式:
# 1. 打开浏览器访问 https://www.tiktok.com 并登录
# 2. 按 F12 打开开发者工具
# 3. 切换到 Application/应用程序 标签
# 4. 左侧找到 Cookies -> https://www.tiktok.com
# 5. 复制所有 Cookie 值，格式如: name1=value1; name2=value2; ...
TIKTOK_COOKIE=

# TikTok API 服务地址 (默认使用本地 Docker 容器)
TIKTOK_API_URL=http://localhost:8000

# API 密钥 (可选 - 如果在 config.yaml 中设置了)
TIKTOK_API_KEY=

# 代理设置 (可选 - 如果需要使用代理访问 TikTok)
HTTP_PROXY=
HTTPS_PROXY=
SOCKS_PROXY=

# 请求延迟设置 (毫秒)
TIKTOK_REQUEST_DELAY=1000

# 并发请求数
TIKTOK_MAX_CONCURRENT=5
